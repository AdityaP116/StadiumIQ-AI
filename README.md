<div align="center">
  <h1>🏟️ StadiumIQ</h1>
  <p>AI-Powered Stadium Operations Platform built for modern, large-scale events.</p>
</div>

---

## 2. Project Overview

**StadiumIQ** is a comprehensive, GenAI-enabled stadium operations platform originally designed for large-scale sporting events like the FIFA World Cup. 

### Your Chosen Vertical
This project focuses on the **Stadium Operations & Smart Infrastructure** vertical. It targets the immense operational complexity of managing 80,000+ fans in real-time, focusing specifically on crowd control, emergency response, and fan assistance.

### Approach and Logic
Managing a stadium involves thousands of moving parts—from crowd bottlenecks and medical emergencies to transport logistics and accessibility routing. Traditional systems are siloed and reactive. Our logic focuses on aggregating live operational data (occupancy, weather, security, medical incidents) and feeding it dynamically as real-time context into advanced Generative AI to provide actionable intelligence.

### How the Solution Works
StadiumIQ centralizes live data and uses a custom React frontend connected to a Node.js Express backend. The backend strictly ensures that live operational data is collected *before* being injected into AI prompts to prevent hallucinations. The platform acts as an executive analyst (dashboard), an emergency responder (auto-generating protocols), and a dynamic fan assistant all in one, using real-time Socket.io updates to keep the frontend completely in sync.

### Any Assumptions Made
- We assume the stadium is heavily instrumented with IoT devices (turnstiles, cameras, sensors) capable of streaming data to our backend.
- We assume users (fans) have mobile connectivity to access the AI assistant.
- We assume AI outputs in emergency situations are treated as "suggestions" requiring a final human-in-the-loop signoff.

**Who It Is Built For:** 
- Stadium Operations Directors & Security Personnel
- Medical Response Units & Crowd Management Teams
- Event Attendees & Fans requiring smart navigation

---

## 3. Features

* 🤖 **AI Fan Assistant**: Natural language Q&A for attendees.
* 🗺 **Smart Navigation**: Dynamic, step-by-step indoor/outdoor routing using natural landmarks.
* 👥 **Crowd Management**: Real-time risk assessment and AI-generated diversion tactics based on live gate occupancy.
* 🚨 **Emergency Response**: Automated, AI-generated emergency response plans logged directly for audit trails.
* 🌍 **Multilingual Support**: AI-generated public address (PA) announcements in multiple languages.
* ♿ **Accessibility Assistance**: Specialized routing and assistance planning for wheelchair users and visually impaired fans.
* 🚉 **Transport Recommendations**: Live transport advisories based on current traffic and metro delays.
* 🌱 **Sustainability Insights**: Eco-friendly operational suggestions dynamically generated by AI.
* 📊 **Live Operations Dashboard**: Executive AI-summarized insights running alongside real-time Socket.io metrics.

---

## 4. Screenshots

*(Screenshots coming soon)*

```text
docs/images/dashboard.png
docs/images/chat.png
docs/images/navigation.png
docs/images/emergency.png
```

---

## 5. Tech Stack

### Frontend
- **React (v18)**
- **Vite**
- **TailwindCSS**
- **Framer Motion** (Animations)

### Backend
- **Node.js (v20+)**
- **Express.js (v5)**

### Database
- **Firebase Firestore** (NoSQL Database)

### AI
- **NVIDIA NIM API (`gpt-oss-20b`)**
- **OpenAI Node.js SDK** (for clean API wrapping)

### Authentication
- **Firebase Authentication**

### Real-time Communication
- **Socket.io** (Broadcasting live stadium updates)

### Deployment
- *Ready for deployment on standard Node.js hosting (Render, Heroku, AWS, Vercel)*

### Version Control
- **Git & GitHub**

---

## 6. Architecture

StadiumIQ relies on a clean, scalable architecture separating presentation from business logic and AI processing. The backend strictly ensures that live operational data is collected *before* being injected into AI prompts to prevent hallucinations.

```text
Frontend (React/Vite)
       ↓
REST API (Axios)
       ↓
Express Server (Controllers & Rate Limiting)
       ↓
Feature Services (Live Data Fetching)
       ↓
Prompt Builders (Context Injection)
       ↓
OpenAI Client SDK (Configured for NVIDIA NIM)
       ↓
Firebase (Firestore Logging & Auth)
       ↓
Real-time Socket.io Updates
```

---

## 7. Folder Structure

```text
StadiumIQ-AI/
├── backend/
│   ├── .env
│   ├── .env.example
│   ├── package.json
│   ├── server.js
│   └── src/
│       ├── config/
│       ├── constants/
│       ├── controllers/
│       ├── middleware/
│       ├── prompts/
│       ├── routes/
│       ├── services/
│       ├── socket/
│       ├── utils/
│       └── validators/
└── frontend/
    ├── .env
    ├── .env.example
    ├── index.html
    ├── package.json
    ├── tailwind.config.js
    ├── vite.config.js
    └── src/
        ├── assets/
        ├── components/
        ├── constants/
        ├── context/
        ├── hooks/
        ├── layouts/
        ├── main.jsx
        ├── pages/
        ├── routes/
        ├── services/
        ├── styles/
        └── utils/
```

---

## 8. Installation

Follow these steps to run the project locally.

### 1. Clone the repository
```bash
git clone https://github.com/AdityaP116/StadiumIQ-AI.git
cd StadiumIQ-AI
```

### 2. Install Dependencies
Open two terminal windows (one for frontend, one for backend).
```bash
# In terminal 1
cd backend
npm install

# In terminal 2
cd frontend
npm install
```

### 3. Configure Environment Variables
Copy the example files and populate them with your keys.
```bash
# Backend
cd backend
cp .env.example .env

# Frontend
cd frontend
cp .env.example .env
```

### 4. Run Backend
```bash
cd backend
npm run dev
```

### 5. Run Frontend
```bash
cd frontend
npm run dev
```

---

## 9. Environment Variables

### Backend (`backend/.env`)

| Variable | Description |
| :--- | :--- |
| `PORT` | Server port (default: 3000) |
| `NODE_ENV` | `development` or `production` |
| `FIREBASE_PROJECT_ID` | Firebase project ID |
| `FIREBASE_CLIENT_EMAIL` | Firebase service account email |
| `FIREBASE_PRIVATE_KEY` | Firebase private key (with `\n`) |
| `NVIDIA_API_KEY` | NVIDIA NIM API Key (via OpenAI SDK) |
| `CLIENT_URL` | Frontend URL for CORS (default: `*`) |

---

## 10. API Documentation

| Method | Route | Description | Auth Required | Request Body Example |
| :--- | :--- | :--- | :--- | :--- |
| **GET** | `/health` | Server health check | No | - |
| **GET** | `/api/debug/genai` | Diagnose AI integration | No | - |
| **POST** | `/api/ai/chat` | Natural language fan assistant | No | `{ "message": "Where is food?", "userLocation": "Gate 2" }` |
| **POST** | `/api/navigation/route`| Get step-by-step directions | No | `{ "currentLocation": "Gate 1", "destination": "Section C" }` |
| **POST** | `/api/crowd/analyze` | AI crowd analysis & risk info | Yes | - |
| **POST** | `/api/dashboard/insight`| AI executive operations report | Yes | - |
| **GET** | `/api/dashboard/summary` | Live data-only summary (fast) | Yes | - |
| **POST** | `/api/emergency/respond`| AI emergency response plan | Yes | `{ "emergencyType": "medical", "zone": "North Stand" }` |
| **POST** | `/api/announcement/generate` | Multilingual PA announcements | Yes | `{ "type": "crowd_control", "languages": ["en", "ar"] }` |
| **POST** | `/api/accessibility/assist` | Accessible route planning | No | `{ "need": "wheelchair", "currentLocation": "Main" }` |
| **POST** | `/api/transport/recommend` | Ranked transport recommendations| No | `{ "userLocation": "Exit", "destination": "City" }` |
| **POST** | `/api/sustainability/tips`| Eco-friendly recommendations | No | `{ "targetAudience": "fans" }` |
| **GET** | `/api/users/profile` | Get authenticated user profile | Yes | - |

*(Note: Authenticated endpoints require `Authorization: Bearer <Firebase_ID_Token>` in the headers).*

---

## 11. Usage

1. **Dashboard Overview:** Upon logging in, administrators see a live dashboard driven by real-time Socket.io updates showing medical incidents, crowd capacity, and weather.
2. **AI Insights:** Clicking "Generate Insight" triggers the backend to bundle current live data, inject it into a dynamic prompt, and fetch a JSON-formatted executive summary from the NVIDIA AI model.
3. **Fan App:** Attendees can ask the AI assistant questions. The system processes their location to give contextual answers regarding the nearest food, exits, or transport options.
4. **Emergencies:** If an incident is reported, the system generates a structured, step-by-step response plan which is permanently logged in Firestore for accountability.

---

## 12. Project Workflow

1. **Data Collection:** `fakeDataGenerator.js` (or live sensors) creates a snapshot of the stadium's current state.
2. **Action Trigger:** A user (Fan or Admin) makes a request via the React frontend.
3. **Routing:** The Express controller validates the request and routes it to the specific Feature Service (e.g., `crowd.service.js`).
4. **Context Building:** The service fetches the live data and passes it to a dedicated Prompt Builder.
5. **AI Inference:** The OpenAI SDK makes an external call to NVIDIA NIM APIs, enforcing JSON outputs.
6. **Delivery:** The structured response is parsed, logged in Firebase if necessary, and sent back to the frontend UI.

---

## 13. AI Integration

- **Provider:** The project uses the **OpenAI Node.js SDK**, configured to point to the **NVIDIA NIM API** (`baseURL: https://integrate.api.nvidia.com/v1`).
- **Model:** Currently utilizing the `openai/gpt-oss-20b` model.
- **Prompt Engineering:** Prompts are stored cleanly in the `src/prompts/` directory. Static text is combined with dynamic JSON representations of live stadium state to prevent hallucinations.
- **Response Handling:** The AI service (`openai.service.js`) handles rate-limiting, exponential backoff retries, extracts `reasoning_content` (for Chain-of-Thought models), and strictly parses JSON responses before returning them to the application logic.

---

## 14. Future Improvements

- [ ] **Real IoT Integration:** Replace the `fakeDataGenerator` with actual live turnstile and camera sensor feeds.
- [ ] **Streaming Responses:** Implement Server-Sent Events (SSE) for streaming the AI chat assistant responses token-by-token.
- [ ] **Vector Database (RAG):** Integrate a vector database to search through thousands of pages of historical stadium operating procedures during emergencies.
- [ ] **Push Notifications:** Tie Firebase Cloud Messaging into the AI announcement generator.

---

## 15. Contributing

Contributions are welcome! To contribute:
1. Fork the repository.
2. Create a feature branch (`git checkout -b feature/AmazingFeature`).
3. Commit your changes (`git commit -m 'Add some AmazingFeature'`).
4. Push to the branch (`git push origin feature/AmazingFeature`).
5. Open a Pull Request.

Please ensure all new routes use the established Controller -> Service -> Prompt Builder architecture.

---

## 16. License

Distributed under the MIT License. See `LICENSE` for more information.

---

## 17. Author

**AdityaP116**  
- GitHub: [@AdityaP116](https://github.com/AdityaP116)  
